<div class="container">
	<div class="row justify-content-center">
		<div class="col-6 col-offset-3">
			<h1>Publicar Producto</h1>
			<%= form_for @product do |f| %>
				<div class="form-group">
					<%= f.text_field :name, class: "form-control", placeholder: "Ingresa el nombre del producto" %>
				</div>
				<div class="form-group">
					<%= f.url_field :url, class: "form-control", placeholder: "Ingresa la URL del producto" %>
				</div>
				<div class="form-group">
					<%= f.text_area :description, class: "form-control", placeholder: "Ingresa la descripción del producto" %>
				</div>

				<div>
					<%= f.submit "Publicar Producto", class: "btn btn-primary btn-lg btn-block" %>
				</div>
			<% end %>
		</div>
	</div>
</div>